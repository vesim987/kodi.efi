name: Buildroot

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: ./build.sh amd64_intel_kodi_defconfig
    - name: make
      run: ./build.sh all
